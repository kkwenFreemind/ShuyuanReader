# Spec-Kit

## 🎯 Spec-Kit 開發方法論

Spec-Kit 是 GitHub 開發的一套**以規格為中心的開發方法**，強調：
1. **規格先行**：先寫規格文檔，再寫代碼
2. **迭代驗證**：每個階段都有可視化結果
3. **測試驅動**：規格即測試用例
4. **持續集成**：自動化驗證

---

## 📋 Spec-Kit 項目結構

讓我創建完整的 Spec-Kit 開發計劃：

````markdown
# 書苑閱讀器 - Spec-Kit 開發計劃

## 🎯 Spec-Kit 方法論

### 核心原則

1. **Spec First（規格優先）**：先寫規格，再寫代碼
2. **Visual Feedback（可視化反饋）**：每個階段都有 UI 可驗證
3. **Incremental（增量開發）**：小步快跑，頻繁交付
4. **Test Driven（測試驅動）**：規格即測試

### 開發流程

```
1. 編寫 Spec (規格)
   ↓
2. 設計 UI Mock (視覺稿)
   ↓
3. 實現代碼
   ↓
4. 可視化驗證
   ↓
5. 測試通過 ✓
   ↓
6. 進入下一個 Spec
```

---

## 📁 項目結構（Spec-Kit 風格）

```
app/
├── specs/                      # 📋 規格文檔目錄
│   ├── README.md              # Spec-Kit 指南
│   ├── 00-project-setup.md    # Spec 0: 項目設置
│   ├── 01-splash-screen.md    # Spec 1: 啟動畫面
│   ├── 02-book-list.md        # Spec 2: 書籍列表
│   ├── 03-book-detail.md      # Spec 3: 書籍詳情
│   ├── 04-download-manager.md # Spec 4: 下載管理
│   ├── 05-reader-view.md      # Spec 5: 閱讀器
│   └── ...
│
├── design/                     # 🎨 設計稿目錄
│   ├── mockups/               # UI 原型圖
│   ├── wireframes/            # 線框圖
│   └── screenshots/           # 實際截圖
│
├── lib/
│   ├── core/
│   ├── data/
│   ├── domain/
│   └── presentation/
│
└── test/
    ├── specs/                 # 📝 基於 Spec 的測試
    │   ├── spec_01_test.dart
    │   ├── spec_02_test.dart
    │   └── ...
    └── golden/                # 🖼️ Golden 測試（UI 快照）
```

---

## 🚀 開發階段規劃

### Phase 1: Foundation（基礎階段）- Week 1-2

#### Spec 0: 項目設置與架構 ✅

**規格文件**: `specs/00-project-setup.md`

**目標**: 搭建項目骨架，配置開發環境

**可視化驗證**: ✅ 空白 APP 能成功運行

**驗收標準**:
- [ ] Flutter 項目創建成功
- [ ] 所有依賴安裝完成
- [ ] 項目結構符合 Clean Architecture
- [ ] 在真機/模擬器上能運行空白 APP
- [ ] GitHub Actions CI/CD 配置完成

**時間**: 1 天

---

#### Spec 1: 啟動畫面（Splash Screen）✅

**規格文件**: `specs/01-splash-screen.md`

**功能需求**:
1. 顯示 APP Logo 和名稱
2. 初始化 Hive 數據庫
3. 檢查網絡連接
4. 3 秒後自動跳轉到書籍列表

**UI 設計**:

```
┌─────────────────────────┐
│                         │
│                         │
│      📚                 │
│   書苑閱讀器             │
│  ShuyuanReader          │
│                         │
│    Loading...           │
│      ━━━━━━━            │
│                         │
└─────────────────────────┘
```

**可視化驗證**: 
- ✅ 顯示 Logo 和名稱
- ✅ 顯示加載動畫
- ✅ 自動跳轉

**測試用例**:
```dart
testWidgets('Splash screen displays correctly', (tester) async {
  await tester.pumpWidget(MyApp());
  
  // 驗證 Logo 存在
  expect(find.text('書苑閱讀器'), findsOneWidget);
  
  // 驗證 Loading 動畫
  expect(find.byType(CircularProgressIndicator), findsOneWidget);
  
  // 等待 3 秒
  await tester.pumpAndSettle(Duration(seconds: 3));
  
  // 驗證已跳轉到 HomePage
  expect(find.byType(HomePage), findsOneWidget);
});
```

**時間**: 1 天

---

#### Spec 2: 書籍列表頁（Book List）✅

**規格文件**: `specs/02-book-list.md`

**功能需求**:
1. 從 GitHub 下載 books.json
2. 解析並顯示書籍列表（GridView）
3. 顯示封面、書名、作者
4. 支持下拉刷新
5. 錯誤處理（網絡失敗時使用緩存）

**UI 設計**:

```
┌─────────────────────────────────┐
│  書苑閱讀器          🔍  ⚙️      │
├─────────────────────────────────┤
│  ┌──────┐  ┌──────┐  ┌──────┐ │
│  │ 📖  │  │ 📖  │  │ 📖  │ │
│  │封面1 │  │封面2 │  │封面3 │ │
│  │書名1 │  │書名2 │  │書名3 │ │
│  │作者1 │  │作者2 │  │作者3 │ │
│  └──────┘  └──────┘  └──────┘ │
│                                 │
│  ┌──────┐  ┌──────┐  ┌──────┐ │
│  │ 📖  │  │ 📖  │  │ 📖  │ │
│  │書名4 │  │書名5 │  │書名6 │ │
│  └──────┘  └──────┘  └──────┘ │
│                                 │
│            ...                  │
└─────────────────────────────────┘
```

**可視化驗證**:
- ✅ GridView 顯示書籍
- ✅ 每本書顯示封面（或佔位圖）
- ✅ 顯示書名和作者
- ✅ 下拉刷新動畫
- ✅ 加載狀態（Shimmer）
- ✅ 錯誤提示（SnackBar）

**狀態設計**:

1. **加載中（Loading）**
```dart
GridView.builder(
  itemBuilder: (context, index) {
    return Shimmer.fromColors(
      child: Container(/* 佔位符 */),
    );
  },
);
```

2. **成功（Success）**
```dart
GridView.builder(
  itemCount: books.length,
  itemBuilder: (context, index) {
    return BookCard(book: books[index]);
  },
);
```

3. **錯誤（Error）**
```dart
Center(
  child: Column(
    children: [
      Icon(Icons.error_outline, size: 64),
      Text('加載失敗'),
      ElevatedButton(
        onPressed: retry,
        child: Text('重試'),
      ),
    ],
  ),
);
```

4. **空狀態（Empty）**
```dart
Center(
  child: Text('暫無書籍'),
);
```

**測試用例**:
```dart
testWidgets('Book list displays correctly', (tester) async {
  // Mock API
  when(mockApiClient.fetchBooksJson()).thenAnswer((_) async => mockBooksJson);
  
  await tester.pumpWidget(MyApp());
  await tester.pumpAndSettle();
  
  // 驗證 GridView 存在
  expect(find.byType(GridView), findsOneWidget);
  
  // 驗證至少顯示 3 本書
  expect(find.byType(BookCard), findsAtLeastNWidgets(3));
  
  // 驗證第一本書的信息
  expect(find.text('一夢漫言'), findsOneWidget);
});
```

**Golden 測試（UI 快照）**:
```dart
testWidgets('Book list golden test', (tester) async {
  await tester.pumpWidget(MyApp());
  await tester.pumpAndSettle();
  
  await expectLater(
    find.byType(HomePage),
    matchesGoldenFile('goldens/home_page.png'),
  );
});
```

**時間**: 3-4 天

---

### Phase 2: Core Features（核心功能）- Week 3-4

#### Spec 3: 書籍詳情頁（Book Detail）✅

**規格文件**: `specs/03-book-detail.md`

**功能需求**:
1. 顯示書籍完整信息（封面、書名、作者、描述）
2. 顯示下載狀態按鈕
   - 未下載：「下載」按鈕
   - 下載中：進度條 + 「暫停」按鈕
   - 已下載：「打開閱讀」按鈕
3. 顯示文件大小和語言

**UI 設計**:

```
┌─────────────────────────────────┐
│  ← 書籍詳情                      │
├─────────────────────────────────┤
│                                 │
│        ┌──────────┐             │
│        │          │             │
│        │  封面圖  │             │
│        │          │             │
│        └──────────┘             │
│                                 │
│     📚 一夢漫言                  │
│     ✍️ 千華寺繼任主持 見月老人    │
│     🌐 繁體中文                  │
│     💾 2.5 MB                   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  📥 下載  (未下載)        │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │  描述                    │   │
│  │  ...                     │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
```

**下載狀態切換**:

```
未下載        下載中         已下載
┌──────┐    ┌──────┐     ┌──────┐
│ 下載  │ →  │ 35%  │  →  │ 打開  │
│ 2.5MB│    │ ⏸ 暫停│     │ 閱讀  │
└──────┘    └──────┘     └──────┘
```

**可視化驗證**:
- ✅ 顯示完整書籍信息
- ✅ 按鈕狀態正確切換
- ✅ 下載進度動畫流暢
- ✅ 點擊按鈕有正確反饋

**測試用例**:
```dart
testWidgets('Download button changes state correctly', (tester) async {
  await tester.pumpWidget(BookDetailPage(book: mockBook));
  
  // 初始狀態：未下載
  expect(find.text('下載'), findsOneWidget);
  
  // 點擊下載按鈕
  await tester.tap(find.text('下載'));
  await tester.pump();
  
  // 驗證狀態變為下載中
  expect(find.byType(CircularProgressIndicator), findsOneWidget);
  expect(find.text('暫停'), findsOneWidget);
  
  // 等待下載完成
  await tester.pumpAndSettle();
  
  // 驗證狀態變為已下載
  expect(find.text('打開閱讀'), findsOneWidget);
});
```

**時間**: 2-3 天

---

#### Spec 4: 下載管理器（Download Manager）✅

**規格文件**: `specs/04-download-manager.md`

**功能需求**:
1. 支持多任務下載（最多 3 個並發）
2. 顯示下載隊列
3. 支持暫停/恢復/取消
4. 顯示下載速度和剩餘時間
5. 下載完成通知

**UI 設計**:

```
┌─────────────────────────────────┐
│  下載管理        清空已完成      │
├─────────────────────────────────┤
│  📥 正在下載 (2)                 │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 📖 金剛經               │   │
│  │ ━━━━━━━━━━━━━━━ 65%    │   │
│  │ 1.2MB/2.5MB  500KB/s   │   │
│  │        ⏸️ 暫停  ❌ 取消  │   │
│  └─────────────────────────┘   │
│                                 │
│  ┌─────────────────────────┐   │
│  │ 📖 心經                 │   │
│  │ ━━━━━━━━━━━━━━━ 32%    │   │
│  │ 0.8MB/1.5MB  450KB/s   │   │
│  │        ⏸️ 暫停  ❌ 取消  │   │
│  └─────────────────────────┘   │
│                                 │
│  📋 等待中 (1)                   │
│  • 楞嚴經                        │
│                                 │
│  ✅ 已完成 (5)                   │
│  • 一夢漫言                      │
│  • 三世因果目蓮救母經             │
│  • ...                          │
└─────────────────────────────────┘
```

**可視化驗證**:
- ✅ 下載隊列實時更新
- ✅ 進度條平滑動畫
- ✅ 下載速度實時顯示
- ✅ 暫停/恢復功能正常
- ✅ 完成通知彈出

**測試用例**:
```dart
testWidgets('Download manager handles concurrent downloads', (tester) async {
  await tester.pumpWidget(DownloadManagerPage());
  
  // 添加 5 個下載任務
  for (var i = 0; i < 5; i++) {
    downloadManager.addTask(mockBooks[i]);
  }
  await tester.pump();
  
  // 驗證只有 3 個正在下載
  expect(find.text('正在下載 (3)'), findsOneWidget);
  
  // 驗證有 2 個等待中
  expect(find.text('等待中 (2)'), findsOneWidget);
});
```

**時間**: 3-4 天

---

#### Spec 5: EPUB 閱讀器（Reader View）✅

**規格文件**: `specs/05-reader-view.md`

**功能需求**:
1. 打開並渲染 EPUB 文件
2. 支持左右滑動翻頁
3. 顯示章節導航
4. 顯示閱讀進度
5. 支持字體大小調整
6. 支持亮度調整

**UI 設計**:

```
┌─────────────────────────────────┐
│  ← 一夢漫言    ┊    ⚙️  🔖       │
├─────────────────────────────────┤
│                                 │
│  第一章 序                       │
│                                 │
│  余於庚午歲，遊居金陵...          │
│  ...                            │
│  ...                            │
│                                 │
│                                 │
│                                 │
│                                 │
│                                 │
│                                 │
│                                 │
│                                 │
│                                 │
│  ━━━━━━━━━━━━━━━━━━━ 15%        │
│      第 5 頁 / 共 30 頁          │
└─────────────────────────────────┘

點擊中央 → 顯示菜單
┌─────────────────────────────────┐
│  [亮度] ◐◐◐◐◑◯◯◯              │
│  [字體] Aa Aa Aa Aa Aa          │
│  [目錄] [書籤] [設置]            │
└─────────────────────────────────┘
```

**可視化驗證**:
- ✅ EPUB 正確渲染
- ✅ 翻頁動畫流暢
- ✅ 進度條實時更新
- ✅ 設置菜單彈出/收起
- ✅ 字體大小實時改變

**測試用例**:
```dart
testWidgets('Reader displays epub content', (tester) async {
  await tester.pumpWidget(ReaderPage(book: mockBook));
  await tester.pumpAndSettle();
  
  // 驗證 EPUB 內容存在
  expect(find.byType(EpubView), findsOneWidget);
  
  // 驗證進度條
  expect(find.text('15%'), findsOneWidget);
  
  // 點擊中央顯示菜單
  await tester.tap(find.byType(EpubView));
  await tester.pump();
  
  // 驗證菜單出現
  expect(find.text('目錄'), findsOneWidget);
  expect(find.text('書籤'), findsOneWidget);
});
```

**時間**: 4-5 天

---

### Phase 3: Enhancement（增強功能）- Week 5-6

#### Spec 6: 搜索與過濾 ✅

**規格文件**: `specs/06-search-filter.md`

**功能需求**:
1. 搜索書名和作者
2. 過濾已下載/未下載
3. 過濾語言
4. 排序（書名、作者、下載時間）

**UI 設計**:

```
┌─────────────────────────────────┐
│  🔍 [搜索書名或作者...]    🎛️   │
├─────────────────────────────────┤
│  找到 15 個結果                  │
│                                 │
│  ┌──────┐  ┌──────┐            │
│  │ 📖  │  │ 📖  │            │
│  │金剛經│  │心經  │            │
│  └──────┘  └──────┘            │
│                                 │
│            ...                  │
└─────────────────────────────────┘

點擊 🎛️ → 過濾菜單
┌─────────────────────────────────┐
│  下載狀態                        │
│  ☑ 全部                         │
│  ☐ 已下載                       │
│  ☐ 未下載                       │
│                                 │
│  語言                           │
│  ☑ 全部                         │
│  ☐ 繁體中文                     │
│  ☐ 簡體中文                     │
│                                 │
│  排序方式                        │
│  ☑ 書名                         │
│  ☐ 作者                         │
│  ☐ 下載時間                     │
│                                 │
│  [重置]      [確定]              │
└─────────────────────────────────┘
```

**可視化驗證**:
- ✅ 實時搜索結果
- ✅ 過濾即時生效
- ✅ 搜索無結果提示
- ✅ 清除搜索按鈕

**時間**: 2-3 天

---

#### Spec 7: 書籤與筆記 ✅

**規格文件**: `specs/07-bookmark-notes.md`

**功能需求**:
1. 添加/刪除書籤
2. 書籤列表
3. 跳轉到書籤位置
4. 添加筆記
5. 查看所有筆記

**UI 設計**:

```
長按文字 → 彈出菜單
┌─────────────────────────────────┐
│  「余於庚午歲，遊居金陵...」      │
│                                 │
│  [📑 書籤] [📝 筆記] [🔍 搜索]   │
└─────────────────────────────────┘

書籤列表
┌─────────────────────────────────┐
│  書籤 (5)                        │
├─────────────────────────────────┤
│  📑 第一章 序                    │
│     「余於庚午歲，遊居金陵...」   │
│     2025-11-06 15:30            │
│                                 │
│  📑 第二章                       │
│     「...」                      │
│     2025-11-05 20:15            │
│                                 │
│            ...                  │
└─────────────────────────────────┘
```

**可視化驗證**:
- ✅ 書籤圖標顯示
- ✅ 筆記輸入框
- ✅ 列表正確顯示
- ✅ 跳轉功能正常

**時間**: 3-4 天

---

#### Spec 8: 設置與主題 ✅

**規格文件**: `specs/08-settings-themes.md`

**功能需求**:
1. 閱讀設置（字體、行距、邊距）
2. 主題切換（日間/夜間/護眼/復古）
3. 存儲管理（查看佔用空間、清除緩存）
4. 關於頁面

**UI 設計**:

```
主題切換
┌─────────────────────────────────┐
│  ☀️ 日間     🌙 夜間            │
│  🌱 護眼     📜 復古            │
└─────────────────────────────────┘

存儲管理
┌─────────────────────────────────┐
│  已下載書籍                      │
│  15 本 / 125.5 MB               │
│                                 │
│  封面緩存                        │
│  100 張 / 15.2 MB               │
│                                 │
│  [清除緩存]                      │
└─────────────────────────────────┘
```

**可視化驗證**:
- ✅ 主題實時切換
- ✅ 字體大小實時預覽
- ✅ 存儲空間正確計算
- ✅ 清除緩存生效

**時間**: 2-3 天

---

## 📊 視覺化驗證工具

### 1. Flutter DevTools

```bash
# 啟動 DevTools
flutter pub global activate devtools
flutter pub global run devtools
```

**用途**:
- 檢查 Widget 樹
- 性能分析
- 網絡請求監控
- 內存使用情況

### 2. Golden Tests（UI 快照測試）

```dart
// test/golden/home_page_test.dart
testWidgets('Home page golden test', (tester) async {
  await tester.pumpWidget(MyApp());
  await tester.pumpAndSettle();
  
  await expectLater(
    find.byType(HomePage),
    matchesGoldenFile('goldens/home_page.png'),
  );
});
```

**運行命令**:
```bash
# 生成 Golden 文件
flutter test --update-goldens

# 驗證 UI 是否變化
flutter test
```

### 3. 集成測試（Integration Tests）

```dart
// integration_test/app_test.dart
void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();
  
  testWidgets('Complete user flow', (tester) async {
    app.main();
    await tester.pumpAndSettle();
    
    // 1. 等待 Splash Screen
    await tester.pumpAndSettle(Duration(seconds: 3));
    
    // 2. 驗證書籍列表顯示
    expect(find.byType(HomePage), findsOneWidget);
    
    // 3. 點擊第一本書
    await tester.tap(find.byType(BookCard).first);
    await tester.pumpAndSettle();
    
    // 4. 驗證詳情頁
    expect(find.byType(BookDetailPage), findsOneWidget);
    
    // 5. 點擊下載
    await tester.tap(find.text('下載'));
    await tester.pumpAndSettle();
    
    // 6. 等待下載完成
    await tester.pumpAndSettle(Duration(seconds: 30));
    
    // 7. 驗證按鈕變為「打開閱讀」
    expect(find.text('打開閱讀'), findsOneWidget);
    
    // 8. 進入閱讀頁
    await tester.tap(find.text('打開閱讀'));
    await tester.pumpAndSettle();
    
    // 9. 驗證閱讀器
    expect(find.byType(ReaderPage), findsOneWidget);
  });
}
```

**運行命令**:
```bash
flutter test integration_test/app_test.dart
```

### 4. 視覺化驗證檢查清單

每個 Spec 完成後，填寫以下檢查清單：

```markdown
## Spec X: [功能名稱] 視覺化驗證

### ✅ UI 呈現
- [ ] 佈局正確
- [ ] 顏色符合設計
- [ ] 字體大小適中
- [ ] 間距合理
- [ ] 響應式適配（不同屏幕）

### ✅ 交互反饋
- [ ] 按鈕點擊有波紋效果
- [ ] 加載狀態顯示
- [ ] 錯誤提示清晰
- [ ] 動畫流暢（60fps）

### ✅ 邊界情況
- [ ] 無數據時的空狀態
- [ ] 網絡錯誤處理
- [ ] 長文本截斷
- [ ] 快速點擊防抖

### ✅ Golden 測試
- [ ] 生成 Golden 文件
- [ ] Golden 測試通過

### ✅ 真機測試
- [ ] 在 3 台不同設備測試
- [ ] 低端設備性能良好
- [ ] 無卡頓和閃爍

### 📸 截圖記錄
- [x] 正常狀態
- [x] 加載狀態
- [x] 錯誤狀態
- [x] 空狀態
```

---

## 🎯 每日視覺化驗證流程

### Morning（上午）

1. **Review 昨天的 Spec**
   ```bash
   git log --oneline -5
   ```

2. **運行所有測試**
   ```bash
   flutter test
   flutter test integration_test/
   ```

3. **檢查 Golden 文件**
   ```bash
   # 如果 UI 有變化，更新 Golden
   flutter test --update-goldens
   ```

### Afternoon（下午）

1. **實現今天的 Spec**
   - 先寫測試
   - 再寫代碼
   - 保持 Red-Green-Refactor 循環

2. **每小時運行一次 Hot Reload**
   ```bash
   # 在真機上運行
   flutter run -d <device_id>
   
   # 修改代碼後按 'r' 熱重載
   ```

3. **記錄視覺化結果**
   - 截圖保存到 `design/screenshots/`
   - 標註日期和 Spec 編號

### Evening（晚上）

1. **完整測試流程**
   ```bash
   # 單元測試
   flutter test
   
   # Widget 測試
   flutter test test/widgets/
   
   # 集成測試
   flutter test integration_test/
   
   # Golden 測試
   flutter test --update-goldens
   ```

2. **真機驗證**
   - 在至少 2 台設備上測試
   - 記錄任何視覺問題

3. **提交代碼**
   ```bash
   git add .
   git commit -m "feat(spec-05): implement reader view with visual tests"
   git push origin main
   ```

---

## 📈 進度追蹤儀表板

創建一個簡單的 Markdown 表格來追蹤進度：

```markdown
## 開發進度儀表板

| Spec | 功能 | 狀態 | 測試 | Golden | 截圖 | 完成日期 |
|------|------|------|------|--------|------|----------|
| 00 | 項目設置 | ✅ | ✅ | - | ✅ | 2025-11-08 |
| 01 | 啟動畫面 | ✅ | ✅ | ✅ | ✅ | 2025-11-09 |
| 02 | 書籍列表 | 🔄 | ⬜ | ⬜ | ⬜ | - |
| 03 | 書籍詳情 | ⬜ | ⬜ | ⬜ | ⬜ | - |
| 04 | 下載管理 | ⬜ | ⬜ | ⬜ | ⬜ | - |
| 05 | 閱讀器 | ⬜ | ⬜ | ⬜ | ⬜ | - |
| 06 | 搜索過濾 | ⬜ | ⬜ | ⬜ | ⬜ | - |
| 07 | 書籤筆記 | ⬜ | ⬜ | ⬜ | ⬜ | - |
| 08 | 設置主題 | ⬜ | ⬜ | ⬜ | ⬜ | - |

**圖例**:
- ✅ 已完成
- 🔄 進行中
- ⬜ 未開始
```

---

## 🎬 視覺化 Demo 錄製

每個 Spec 完成後，錄製一個短視頻展示功能：

```bash
# 使用 Android Studio 的 Screen Record
# 或使用 scrcpy
scrcpy --record=spec-02-book-list.mp4
```

**視頻命名規範**:
```
demo/
├── spec-01-splash-screen.mp4
├── spec-02-book-list.mp4
├── spec-03-book-detail.mp4
└── ...
```

---

## 📚 Spec 文檔模板

每個 Spec 文檔應包含：

```markdown
# Spec XX: [功能名稱]

## 📋 概述
簡要描述這個功能是什麼

## 🎯 目標
- 目標 1
- 目標 2
- 目標 3

## 📐 UI 設計
[ASCII 藝術圖 或 設計稿連結]

## 🔧 功能需求
1. 需求 1
2. 需求 2
3. 需求 3

## ✅ 驗收標準
- [ ] 標準 1
- [ ] 標準 2
- [ ] 標準 3

## 🧪 測試用例
\`\`\`dart
testWidgets('test name', (tester) async {
  // 測試代碼
});
\`\`\`

## 🎨 視覺化驗證
- [ ] UI 呈現正確
- [ ] 交互流暢
- [ ] Golden 測試通過
- [ ] 真機測試通過

## 📸 截圖
- 正常狀態：[連結]
- 加載狀態：[連結]
- 錯誤狀態：[連結]

## 📹 Demo 視頻
[視頻連結]

## ⏱️ 預估時間
X 天

## 📝 實際時間
Y 天

## 💡 遇到的問題
問題 1：...
解決方案：...

## 🔗 相關 Spec
- Spec XX: [名稱]
- Spec YY: [名稱]
```

---

## 🚀 開始第一個 Spec

讓我們從 **Spec 0** 開始！
````
